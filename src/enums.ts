export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
  SYSTEM = 'system',
}

export enum OrderStatus {
  OPEN = 'open',
  PROCESSING = 'processing',
  CLOSED = 'closed',
  PENDING = 'pending',
  DISPATCHED = 'dispatched',
}
